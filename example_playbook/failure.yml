---
- hosts: apacheweb
  user: test
  sudo: yes
  connection: ssh
  gather_facts: no
  tasks:
    - name: Execute a command that will fail
      ansible.builtin.command: /bin/false
      ignore_errors: yes

    - name: Install telnet
      ansible.builtin.yum:
        name: telnet
        state: latest
